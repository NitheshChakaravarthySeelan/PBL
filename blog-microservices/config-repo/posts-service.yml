server:
  port: 8082

spring:
  application:
    name: posts-service
  jwt:
    secret: afafafafafafafafafafafafafafafafafafafafafafafafafafafafafafafaf
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:postsdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost